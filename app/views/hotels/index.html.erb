<h1>Hotels</h1>

<ul>
  <% @hotels.each do |hotel| %>
    <li><%= link_to hotel.name, hotel_path(hotel) %></li>
  <% end %>
</ul>

<%= current_user %>
<% if !current_user.nil? %>
  <%= link_to 'Add new hotel', new_hotel_path %>
<% end %>
